<% if (mode.includes('dev')) { %>
<script>
  const port = <%= port %>; // Safely inject port
  const ws = new WebSocket(`ws://127.0.0.1:${port + 1}`);
  ws.onclose = () => {
      setTimeout(() => location.reload(), 2000);
  };
</script>
<% } %>

<nav>
  <a href="/">Home</a>
  <a href="/about">About</a>
  <a href="/contact">Contact</a>
</nav>
